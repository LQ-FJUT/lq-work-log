# L.Q记工本：项目介绍与使用指南

## 项目作用

L.Q记工本是一款完全离线的 Windows 记工与工资统计工具，适合个人或小团队记录工人、每日出工、加班、请假、工地归属、补贴扣款和结清状态，并生成月度/年度统计、Excel 与打印/PDF 报表。

当前发布版本为 **v2.2.1**。

## 安装

1. 在 GitHub 仓库打开 **Releases**。
2. 下载 `L.Q记工本-2.2.1-Setup.exe`。
3. 运行安装程序并完成当前用户安装。

安装包未签名，Windows 可能提示未知发布者。请先核对 Release 页面的 SHA-256。

## 基本使用

1. 添加单个工人，或使用批量添加一次录入多人及日薪。
2. 在月视图点击或拖动考勤格；也可用方向键、数字键和 `Backspace` 快速录入。
3. 在当日详情中填写备注、请假和实际工地。
4. 使用当日总览、工地统计和年度汇总核对人员与成本。
5. 在补贴扣款中记录额外金额，并按需要标记发薪结清。
6. 使用 JSON 导出/恢复完成备份或跨电脑迁移；Excel 与 PDF 仅用于报表。

## 数据与备份

正式数据保存在 `%APPDATA%\L.Q记工本\data`，与安装目录分离。应用采用校验、串行保存和原子替换；默认每周向 Windows“文档\L.Q记工本备份”写入外部 JSON 备份。卸载程序不会主动删除用户数据，但重要操作前仍建议手动导出完整 JSON。

## 从源码运行

需要 Node.js 22.12 或更高版本。

```powershell
npm ci
npm test
npm run typecheck
npm run build
npm run test:e2e
npm run desktop:start
```

构建正式 Windows 安装包：

```powershell
npm run desktop:make
```

## 发布状态

本仓库保存 v2.2.1 最终源码，安装包通过 GitHub Release 分发。本地发布链、ASAR 审计和桌面 smoke 不等同于无 Node.js 的干净 Windows 首装、从 2.2.0 升级、卸载及数据保留验收。
